[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "cyclesense"
version = "0.1.0"
description = "Research-informed menstrual cycle prediction using machine learning"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["src"]